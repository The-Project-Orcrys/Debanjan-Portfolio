# Footer background video

Place a looping workspace/desk video here as `footer-texture.mp4` (muted, short loop, compressed).

The footer uses `/videos/footer-texture.mp4` by default, or set `footerVideoUrl` in Sanity site settings.
