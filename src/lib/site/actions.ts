/**
 * Client-safe calendar URL. For résumé, import from `@/lib/site/resume.server`
 * in Server Components only (uses filesystem).
 */
export { getCalendarUrl, getCalendarEmbedUrl } from "@/lib/site/calendar";
